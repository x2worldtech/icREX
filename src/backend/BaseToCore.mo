// BaseToCore.mo
// Migration helpers — mo:base dependencies removed since actor state already uses mo:core.
// Kept as a no-op placeholder so existing imports do not break during the transition.
module {};
